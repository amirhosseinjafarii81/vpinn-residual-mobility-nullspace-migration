# Stage Index

| Stage | Main question | Outcome |
|---|---|---|
| 1-5 | Is the high-frequency VPINN plateau reproducible and finite-horizon? | Reproducible; m=9 lock replicated, but long-horizon escape occurs. |
| 6-9 | Does Adam geometry / target reflection explain or repair it? | Adam can move target-uphill; one-step reflection helps locally; repeated reflection destabilizes other weak equations. |
| 10-16 | Is there a safe Pareto direction that accelerates escape? | Local safe compromises exist, but the final matched-state acceleration claim fails. |
| 17R | What explains failed local repairs? | Parameter-space coupling, optimizer state and nonlinear/GN curvature matter. |
| 18R-21R | Is the phenomenon simply frequency/parity? | Pure frequency/parity explanation rejected. |
| 22R-25R | Is the deep phenotype specific to paired base-mode conditions? | Strong paired specificity in the tested family; horizon censoring closed. |
| 26R | Residual direction or kernel shape alone? | Neither one-factor explanation is sufficient; joint residual/kernel geometry required. |
| 27R | Does mobility collapse precede optimizer conflict? | Yes in 5/5 seeds; median lead 1475 epochs. |
| 28R | Does the phenotype transfer to reaction-diffusion? | Yes for the paired deep-lock phenotype. |
| 29R | Does it transfer to a non-Fourier P1 test space? | Deep lock transfers; strict b=2 escape gate fails because two controls hit a different error floor. |
| 30R | Why did the two b=2 controls fail to escape? | Distributed weak-test nullspace / near-nullspace invisibility. |
| 31R | Does minimal P1 refinement expose and reduce hidden error? | Visibility restored in both endpoints; trajectories improved; strict rescue 1/2. |
| 32R | Did the error migrate into the new complement? | Yes for the unresolved refined endpoint; discarded V25 sees the new error again. |
| 33R | Does persistent union information help? | Union restores visibility and certifies escape; late-time trajectory still spikes. |
| 34R | Why do the late union spikes occur? | Both precommitted windows are Gauss-Newton-dominated curvature overshoot. |
